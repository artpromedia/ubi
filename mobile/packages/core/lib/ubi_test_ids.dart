/// The testID registry, mirrored from `packages/contracts/src/test-ids.ts`.
///
/// Its own entry point so a Maestro flow generator or a widget test can import
/// the ids without pulling in the rest of the package.
library;

export 'src/testing/test_ids.dart';
