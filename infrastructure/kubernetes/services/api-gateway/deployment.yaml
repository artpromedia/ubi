# =============================================================================
# API Gateway Deployment
# =============================================================================
# The API Gateway is the entry point for all client requests.
# It handles routing, authentication, rate limiting, and request transformation.
#
# SCALE-OUT TRACK ONLY. The pilot runs on the Hetzner single-host compose
# stack (infrastructure/hetzner/, docs/ops/PILOT_RUNBOOK_DELTAS.md). This is
# the only service with a Kubernetes manifest; the services its URLs name
# (ask, travel, fleet, ...) have none yet, and none of this has been applied
# to a cluster. The environment below mirrors what the gateway reads
# (docs/ops/DEPLOY_ENV_MATRIX.md) so the manifest is not wrong by omission.
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: ubi-apps
  labels:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/component: gateway
    app.kubernetes.io/part-of: ubi
    app.kubernetes.io/version: "1.0.0"
spec:
  replicas: 3
  revisionHistoryLimit: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: api-gateway
  template:
    metadata:
      labels:
        app.kubernetes.io/name: api-gateway
        app.kubernetes.io/component: gateway
        app.kubernetes.io/part-of: ubi
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: api-gateway
      priorityClassName: ubi-high

      # Pod anti-affinity for HA
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: api-gateway
                topologyKey: kubernetes.io/hostname
            - weight: 50
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: api-gateway
                topologyKey: topology.kubernetes.io/zone

      # Topology spread for even distribution
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: api-gateway

      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault

      containers:
        - name: api-gateway
          image: ${ECR_REGISTRY}/ubi/api-gateway:${IMAGE_TAG}
          imagePullPolicy: IfNotPresent

          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP

          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3000"
            - name: LOG_LEVEL
              value: info
            - name: LOG_FORMAT
              value: json

            # Service discovery. Every proxied service needs its URL: an unset
            # one falls back to a localhost default and its routes answer 503.
            - name: USER_SERVICE_URL
              value: "http://user-service.ubi-apps:3000"
            - name: RIDE_SERVICE_URL
              value: "http://ride-service.ubi-apps:8080"
            - name: FOOD_SERVICE_URL
              value: "http://food-service.ubi-apps:3000"
            - name: DELIVERY_SERVICE_URL
              value: "http://delivery-service.ubi-apps:8080"
            - name: PAYMENT_SERVICE_URL
              value: "http://payment-service.ubi-apps:3000"
            - name: NOTIFICATION_SERVICE_URL
              value: "http://notification-service.ubi-apps:3000"
            # Round 9: the gateway also proxies ask (/v1/ask), travel
            # (/v1/travel/..., /v1/reservations, /v1/ops/travel) and fleet
            # (/v1/fleets, /v1/fleet-offers, /v1/drivers/me/fleet...). Ports are
            # each service's own default.
            - name: ASK_SERVICE_URL
              value: "http://ask-service.ubi-apps:4013"
            - name: TRAVEL_SERVICE_URL
              value: "http://travel-service.ubi-apps:4012"
            - name: FLEET_SERVICE_URL
              value: "http://fleet-service.ubi-apps:4015"

            # Who the client is: X-Forwarded-For is believed only from these
            # addresses (the ingress controller). From the ConfigMap; an overlay
            # must narrow it to the ingress's own addresses. Empty trusts no one.
            - name: GATEWAY_TRUSTED_PROXIES
              valueFrom:
                configMapKeyRef:
                  name: api-gateway-config
                  key: GATEWAY_TRUSTED_PROXIES

            # Internal trust boundary (docs/security/INTERNAL_IDENTITY.md). The
            # gateway refuses to start without them. The Secret is provisioned
            # outside this repository (never committed).
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: api-gateway-secrets
                  key: jwt-secret
            - name: UBI_IDENTITY_SECRET
              valueFrom:
                secretKeyRef:
                  name: api-gateway-secrets
                  key: ubi-identity-secret
            - name: RIDE_INTERNAL_CONTEXT_SECRET
              valueFrom:
                secretKeyRef:
                  name: api-gateway-secrets
                  key: ride-internal-context-secret
            # The gateway reads ONE Redis URL (rate limits, token revocation);
            # the REDIS_HOST/PORT/PASSWORD triple below is kept for the shared
            # redis-credentials Secret's other consumers.
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: redis-credentials
                  key: url

            # Redis connection
            - name: REDIS_HOST
              valueFrom:
                secretKeyRef:
                  name: redis-credentials
                  key: host
            - name: REDIS_PORT
              valueFrom:
                secretKeyRef:
                  name: redis-credentials
                  key: port
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: redis-credentials
                  key: password

            # OpenTelemetry
            - name: OTEL_SERVICE_NAME
              value: api-gateway
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector.ubi-monitoring:4317"
            - name: OTEL_TRACES_SAMPLER
              value: parentbased_traceidratio
            - name: OTEL_TRACES_SAMPLER_ARG
              value: "0.1"

          envFrom:
            - configMapRef:
                name: ubi-config

          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi

          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL

          # Health checks
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3

          startupProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 30

          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: cache
              mountPath: /app/.cache

      volumes:
        - name: tmp
          emptyDir: {}
        - name: cache
          emptyDir: {}

      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: ubi-apps
  labels:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/component: gateway
    app.kubernetes.io/part-of: ubi
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
    - name: metrics
      port: 9090
      targetPort: metrics
      protocol: TCP
  selector:
    app.kubernetes.io/name: api-gateway
---
# Horizontal Pod Autoscaler
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: api-gateway
  namespace: ubi-apps
  labels:
    app.kubernetes.io/name: api-gateway
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: api-gateway
  minReplicas: 3
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 10
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
        - type: Percent
          value: 100
          periodSeconds: 15
        - type: Pods
          value: 4
          periodSeconds: 15
      selectPolicy: Max
---
# Pod Disruption Budget
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: api-gateway
  namespace: ubi-apps
  labels:
    app.kubernetes.io/name: api-gateway
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: api-gateway
