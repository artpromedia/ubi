# Build output and dependencies
node_modules
dist
.next
.turbo
coverage
build

# Generated code (owned by their generators, not hand-formatted)
**/*.g.dart
**/*.freezed.dart
**/*.config.dart
**/*.mocks.dart
packages/database/prisma/migrations
pnpm-lock.yaml

# Vendored verbatim from the external design handoff — kept byte-for-byte
docs/launch-readiness/handoff

# Vendored verbatim from the external marketing design handoff (board 24)
docs/launch-readiness/handoff-marketing

# Supplier API documentation example payloads, copied verbatim from the
# official Duffel / LiteAPI docs (each file records its source URL and
# retrieval date) so they can be diffed against the source — keep byte-for-byte
services/travel-service/tests/fixtures/supplier-docs
