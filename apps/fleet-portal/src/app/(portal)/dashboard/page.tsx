"use client";

import { OverviewScreen } from "@/components/overview/Overview";

/** The fleet overview (server counts). The portal-wide states live in FleetGate. */
const DashboardPage = () => <OverviewScreen />;

export default DashboardPage;
