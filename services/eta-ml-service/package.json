{
  "name": "eta-ml-service",
  "version": "1.0.0",
  "description": "ML-powered ETA prediction service using XGBoost",
  "private": true,
  "scripts": {
    "setup": "bash setup.sh",
    "setup:deps": "pip install -r requirements.txt",
    "dev": "uvicorn app.main:app --reload --port 4020",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 4020",
    "train": "python -m app.training.train_model",
    "test": "echo 'Run pytest after setting up Python environment'",
    "lint": "echo 'Run ruff after setting up Python environment'",
    "format": "ruff format app/ tests/",
    "typecheck": "echo 'Run mypy after setting up Python environment'",
    "test:py": "pytest tests/ -v",
    "lint:py": "ruff check app/ tests/",
    "typecheck:py": "mypy app/"
  },
  "keywords": [
    "eta",
    "machine-learning",
    "xgboost",
    "prediction"
  ],
  "engines": {
    "python": ">=3.11"
  }
}
