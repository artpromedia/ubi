# Closing the gaps — marketing (board 24)

| # | Gap | Steps | Evidence |
|---|---|---|---|
| 1 | Unverified build | Copy `src/**` into `apps/marketing-site/src` renaming `-cityId-` → `[cityId]`. Merge `tokens/marketing.css` variables with the homepage branch: keep one `--mk-*` set, delete duplicates. Verify `@ubi/ui` exports (`Button, Card, Badge, Input, Label`) against `packages/ui/src/index.ts`; where a name differs, import the real one, do not re-implement. `pnpm -F @ubi/marketing-site typecheck && build` in the monorepo. | Green typecheck + `next build` in CI, not isolated |
| 2 | `/cities` mobile not drawn | Apply 24b rules: single column, launch cards stacked (Lagos then Abuja), planned chips wrap, header/menu from `SiteHeader`. No new components. | Playwright screenshot at 390 attached to PR; no horizontal scroll at 320 |
| 3 | Abuja config missing | In config-service add `seed/abuja.ts` mirroring `lagos.ts`: cityId `ABV`, region FCT, timezone Africa/Lagos, NGN, classes go/comfort/xl, payment methods as Lagos, airport code ABV with Nnamdi Azikiwe doors (from ops), emergency 112, serviceFeePct per finance. Fares provisional until finance sign-off. Both cities `launching` until launch day. | Seed test asserts LOS and ABV parity of required fields |
| 4 | `City.status` + requirements endpoint | config-service: migration adding `status` enum (planned, launching, active, paused) with `active` derived; expose on `GET /v1/config/cities`; seed the eight planned rows (id, name, region, timezone). Add an activation guard: activating one launch city while the other is not active fails with `launch_pair_incomplete`. user-service: `GET /v1/kyc/requirements?cityId=&role=driver` from existing driver document types (Lagos: drivers_licence, lasdri_card, nin_identity + liveness, vehicle_registration, roadworthiness, third_party_insurance, background_check). | Contract tests for both endpoints; guard test; site renders list from the endpoint, never from a constant |
| 5 | Photo licensing | Download originals from the Commons file pages in `src/lib/city-images.ts` to `/public/marketing/cities/los.jpg`, `abv.jpg`; keep `credit`/`creditHref`; if replacing with UBI-owned photos, drop the credit and update alt/caption in config `hero`. | Credit visible on each slide in screenshots; licence recorded in `THIRD_PARTY_NOTICES` |
| 6 | Tests not run | Implement `ACCEPTANCE.md` Playwright list with mocked config-service responses (ok, DENY_ALL, unknown city, launching, planned, env present/missing) at 1440 and 390; axe on each route; Lighthouse a11y ≥ 95. | CI job `marketing-e2e` required, no continue-on-error |
